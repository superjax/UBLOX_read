If you are cloning this repository you will need to get the submodules seperately.  This can be done with the following command.
in UBLOX_read

git submodule update --init --recursive

You will then need to build to run
still in UBLOX_read,
mkdir build
cd build
cmake ..
make -j4 -l4
